[defaults]
inventory = aws_ec2.yml
host_key_checking = False
remote_user = ubuntu
private_key_file = ~/vockey.pem 

[ssh_connection]
ssh_args = -o ProxyCommand="ssh -W %h:%p -q ubuntu@3.231.160.248 -i ~/vockey.pem"
